info:
  name: Create Session
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
